<div class="modal-dialog">
  	<div class="modal-content">
	    <div class="modal-header">
	      <button type="button" class="close" data-dismiss="modal">×</button>
	      <h4 class="modal-title">Edit Training Evaluations</h4>
	    </div>
	    <div class="modal-body form-group form-admin">
	        <ul class="errors alert-danger"></ul>
				<%= form_for @evaluation, :url => admin_evaluation_path do |form| %>
  					<% if @evaluation.errors.any? %>
				        <div id="error_explanation">
				          <h2>
				            <%= pluralize(@evaluation.errors.count, "error") %> prohibited
				            this evaluation from being saved:
				          </h2>
				          <ul>
				            <% @evaluation.errors.full_messages.each do |msg| %>
				              <li><%= msg %></li>
				            <% end %>
				          </ul>
				        </div>
				    <% end %>
			    <div class="form-group">
			        <label>Training Title</label>
			        <%= form.text_field :title, class: "form-control", required: true %>
			    </div>
			    <div class="form-group">
			        <label>Training Evaluations Description</label>
			        <%= form.cktext_area :description, class: "form-control" %>
			    </div>
			    <%= form.submit "Update", class: "btn btn-success btn-sm pull-right" %>
			<% end %>
		</div>
	</div>
</div>
<%= link_to 'Show', admin_evaluation_path(@evaluation) %> |
<%= link_to 'Back', admin_evaluations_path %>
